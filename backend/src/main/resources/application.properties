# =========================================
# 🔧 APPLICATION CONFIGURATION
# =========================================
spring.application.name=MoneyFollow

# =========================================
# 🌍 SERVER CONFIGURATION
# =========================================
server.port=8000

# =========================================
# 🗄️ DATABASE CONFIGURATION (LOCAL)
# =========================================
spring.datasource.url=jdbc:postgresql://localhost:5432/moneyfollow
spring.datasource.username=moneyfollow_user
spring.datasource.password=postgres
spring.datasource.driver-class-name=org.postgresql.Driver

# Optionnel mais utile pour éviter erreurs SSL :
spring.datasource.hikari.data-source-properties.ssl=false

# =========================================
# 🧠 JPA / HIBERNATE CONFIGURATION
# =========================================
# create-drop = recrée la base à chaque démarrage (utile pour dev)
# update = conserve les données et modifie le schéma si besoin
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# =========================================
# 🔐 JWT CONFIGURATION
# =========================================
jwt.secret=your-secret-key-change-this-in-production
jwt.expiration=86400000

# =========================================
# 🪵 LOGGING CONFIGURATION
# =========================================

# Niveau global : affichage uniquement des erreurs
logging.level.root=ERROR

# Niveau spécifique pour Spring et Hibernate
logging.level.org.springframework=ERROR
logging.level.org.hibernate=ERROR

# Mais garde ton code applicatif en DEBUG (pour tes logs à toi)
logging.level.com.moneyfollow=DEBUG